# 캐스케이드 배포 가능성 — 1시간 크론이면 되는가 / 마찰은 얼마나 견디는가

**실행**: 2026-09-01, Actions run #3 (`validate_cascade_realistic.py`)
**표본**: 1h 1,100일 × 56종목, 캐스케이드 신호 312건 (1차 검증과 동일 집합)

| 질문 | 답 |
|---|---|
| A. 1시간 크론으로 바꾸면 게이트를 통과하는가 | **통과한다** (+1.54%, 중앙 +0.31%) |
| B. 상시 서버가 필요한가 | **불필요하다** — 크론만 바꾸면 된다 (비용 0) |
| C. 마찰은 얼마나 견디는가 | **왕복 0.4%까지** (동결 가정 0.2%의 2배) |

---

## 0. 먼저, 앞서 보고한 지연 수치를 정정한다

이번 실행 로그에서 실측 분포를 다시 계산한 결과, 제가 이전에 보고드린 값 중
**여러 개가 틀렸습니다.**

| 항목 | 앞서 보고 | **실제** |
|---|---|---|
| 중앙값 | 16.0분 | **25.1분** |
| 평균 | 44.6분 | **40.7분** |
| p75 | 33.2분 | **37.5분** |
| **p90** | **172.9분** | **91.5분** |
| p95 | — | 188.3분 |
| 최대 | 231.5분 | 231.5분 (일치) |
| 60분 이내 | 82% | 82% (일치) |

특히 **p90 을 173분이라고 했는데 실제로는 91.5분**이고, 173분은 p95 근처입니다.
결론(1시간 크론 채택)은 바뀌지 않지만, 꼬리가 제가 말씀드린 것보다 **얇습니다** —
서버의 가치를 제가 과대평가했던 셈입니다.

---

## 1. 이 시험이 필요했던 이유

`validate_cascade_delay.py`(2026-09-01 1차)는 지연을 **고정값**으로 넣었다.
4h 격자에 지터 10/30/60/90분을 각각 **전 신호에 동일하게** 씌우는 방식이다.

실제 Actions 큐 지연은 고정이 아니라 꼬리가 두꺼운 분포다. 평균 40.7분을 전 신호에
씌우는 것과, 82%는 25분 안에 들어오고 18%는 1~4시간 늦는 혼합을 씌우는 것은 전혀
다른 실험이다. 후자가 실제로 일어날 일이다.

## 2. 결과 A — 크론 주기 비교

실측 100건을 신호마다 부트스트랩 추출(시드 고정)해 씌웠다.

| 조건 | n | 평균 | 중앙 | boot_p | OOS | 평균지연 | d≤1 비율 | 판정 |
|---|---|---|---|---|---|---|---|---|
| 이상 d=0 (1차 검증 조건) | 312 | +2.43% | +1.17% | 0.000 | 3/4 | 0.00봉 | 100% | **PASSED** |
| 이상 d=1 (감쇠 통과선) | 312 | +1.88% | +0.46% | 0.000 | 2/4 | 1.00봉 | 100% | **PASSED** |
| **4h(현행) + 실측지연** | 312 | +0.58% | **−0.37%** | 0.005 | 3/4 | 2.91봉 | 12% | **REJECTED** |
| **1h(제안) + 실측지연** | 312 | **+1.54%** | **+0.31%** | 0.000 | 2/4 | 1.34봉 | **81%** | **PASSED** |
| 1h + 서버(지연 1분) | 312 | +1.88% | +0.46% | 0.000 | 2/4 | 1.00봉 | 100% | PASSED |

**1시간 크론이 게이트를 통과한다.** 현행 4h 는 중앙값이 음수(−0.37%)로 기각인데,
크론만 바꾸면 중앙값이 +0.31% 로 올라온다. 결정적 차이는 `d≤1 비율` 이다 —
**12% → 81%.**

## 3. 서버의 가치는 얼마인가 — 그리고 이 시험이 답할 수 없는 것

`1h + 서버` 행이 `이상 d=1` 과 **숫자가 완전히 같다**(+1.88%, +0.46%). 우연이 아니라
**프레임의 해상도 한계**다. 동결 라벨은 1h 봉보다 잘게 볼 수 없어서, 지연이 1분이든
59분이든 똑같이 "다음 봉 종가 체결(d=1)"로 처리한다.

그래서 표는 이렇게 읽어야 한다:

- **d=0 (+2.43%)** = 서버의 **상한**. 신호봉 종가에 즉시 체결되는 이상적 경우
- **d=1 (+1.88%)** = 60분 내 모든 지연의 **하한**. 서버든 크론이든 공통
- 진짜 값은 **d=0 과 d=1 사이**. 서버는 d=0 쪽에, 크론(중앙 25분)은 그보다 아래

즉 **이 시험은 "한 시간 안쪽에서 서버가 얼마나 더 빠른가"를 측정하지 못한다.**
측정할 수 있는 서버의 기여분은 **꼬리 제거** 하나뿐이고, 그 값이 +1.54% → +1.88%,
즉 **건당 +0.34%p** 다.

정직하게 말하면 서버가 무가치한 것은 아니다. 상한까지 고려하면 서버의 가치는
건당 **+0.34%p ~ +0.89%p** 사이 어딘가다. **다만 크론만으로도 게이트를 넘으므로,
배포에 서버가 필요하지는 않다.**

## 4. 결과 B — 마찰 민감도

캐스케이드는 정의상 강제청산이 연쇄되는 순간이라 스프레드가 가장 벌어져 있다.
배리어가 ±1.5×ATR(0.75~1.5%)로 좁아 마찰에 취약할 수 있다는 우려를 검증했다.

| 왕복 수수료 | d=0 평균 | d=0 중앙 | d=0 판정 | 1h크론 평균 | 1h크론 중앙 | 1h크론 판정 |
|---|---|---|---|---|---|---|
| **0.2% (동결값)** | +2.43% | +1.17% | PASSED | **+1.54%** | **+0.31%** | **PASSED** |
| 0.3% | +2.33% | +1.07% | PASSED | +1.44% | +0.21% | PASSED |
| **0.4%** | +2.23% | +0.97% | PASSED | +1.34% | **+0.11%** | **PASSED (한계)** |
| 0.6% | +2.03% | +0.77% | PASSED | +1.14% | **−0.09%** | REJECTED |
| 0.8% | +1.83% | +0.57% | PASSED | +0.94% | −0.29% | REJECTED |

**1h 크론의 마찰 내성은 왕복 0.4%** — 동결 가정(0.2%)의 2배다. OKX taker 실비가
왕복 0.1% 이므로 슬리피지로 0.3%p 를 더 먹어도 버틴다.

무너지는 것은 항상 **중앙값**이다(0.4%에서 +0.11%, 0.6%에서 −0.09%). 평균은
0.8%에서도 +0.94%로 양수다 — 즉 마찰이 커지면 "평균은 벌지만 절반 이상 지는"
소수 대박 의존 형태로 변질된다. 1차 검증에서 확인했던 건전한 분포가 무너지는 것이라
게이트가 이를 정확히 잡아낸다.

**판정 기준 하나를 교정했다.** `intraday_lab.evaluate` 의 `fee_ok` 는 동결 `FEE`(0.2%)를
고정 잣대로 쓴다. 수수료를 올린 arm 에서는 "그 수수료를 내고도 남는가"로 봐야
일관되므로 `verdict_at_fee` 를 따로 계산해 위 표에 썼다.

## 5. 남은 위험 — 여유가 크지 않다

통과이긴 하지만 **경계에 가깝다.** 정직하게 적어둔다.

1. **중앙값 +0.31%** — 게이트 문턱(0)에서 0.31%p 위. d=0 의 +1.17% 대비 27% 수준
2. **OOS 2/4** — 통과 최소 기준(≥2)에 정확히 걸쳐 있다. d=0 은 3/4 였다
3. **마찰 여유 0.4%까지** — 실비 0.1% 대비 여유는 있으나 캐스케이드 국면의
   실제 슬리피지는 미측정이다. 배포 후 체결가 실측으로 확인해야 한다
4. **Q4 약세** — 1차 검증에서 최근 분기 n=27 mean −0.26% 였다. 이 시험도 같은
   데이터라 같은 약점을 공유한다
5. **지연 분포는 과거 100건** — Actions 큐가 앞으로도 같다는 보장은 없다

## 6. 배포하려면 남은 것 — 크론 변경이 단순하지 않다

**`cron: '0 */4 * * *'` 을 `'0 * * * *'` 로 바꾸는 것만으로는 안 된다.**

`scheduler.py` 는 채택 패턴을 `rows[last]`, 즉 **현재 형성 중인 봉**에서 탐지한다
(`last = len(rows)-1`). 그리고 중복 진입 방어 키가
`(symbol, pattern, direction, date)` 로 **날짜 단위**다.

여기서 문제가 생긴다. 크론을 시간당으로 바꾸면 **1d/4h 배포 패턴의 탐지 기회가
하루 6회에서 24회로 늘어난다.** 중복 방어 덕에 하루 1회 진입 상한은 유지되지만,
**그 1회가 하루 중 더 이른 시각에, 더 덜 형성된 일봉에서 잡히게 된다.**
장중에 잠깐 패턴 모양이 났다가 종가에는 아닌 봉으로 들어갈 확률이 올라간다.

이건 이미 배포된 `engulfing`/`fvg`/`inverted_hammer`/`marubozu`/하모닉 4h/`bat_1h`/
`butterfly_1h` 의 **진입 신호 분포가 검증 당시와 달라진다**는 뜻이다. 레포 원칙
("검증 당시와 달라진다")에 정면으로 걸린다.

그래서 크론 변경은 **1h 탐지만 시간당으로 돌리고 1d/4h 는 기존 6회 틱에만 돌도록
분리**해야 한다. 두 가지 방법이 있다:

| 방안 | 내용 | 장단 |
|---|---|---|
| **(A) 모드 분기** (권장) | 크론을 `0 * * * *` 로 바꾸되, `scheduler.py` 가 UTC 시각이 00/04/08/12/16/20 이 아니면 1d/4h 탐지 블록을 건너뛴다 | 워크플로 1개 유지, 동시 실행 위험 없음. 청산·`ensure_stop_orders` 는 매시 돌아 오히려 개선 |
| (B) 워크플로 분리 | 1h 전용 워크플로를 매시 추가 | 포지션 DB 경합 위험 — `concurrency` 그룹을 공유해 직렬화해야 함 |

**(A) 를 권장한다.** 변경 범위가 작고, 기존 패턴의 탐지 시각 집합이 **정확히 그대로**
유지됨을 테스트로 고정할 수 있다.

## 7. 조치

- `registry.json` — `cascade_realistic_2026_09` 기록, `cascade_fade_long_1h` 의
  `not_deployed_reason` / `activation_checklist` 를 이 결과로 갱신
- **`status` 는 `passed_not_deployed` 유지** — 크론 분리(6절)가 선행돼야 하고,
  실거래 활성화는 사용자 판단 사항이다
- `research_log.csv` 기록
- **실거래 진입·청산 규칙 변경 없음**

## 8. 남은 판단 (사용자)

기술적 blocker 는 사라졌다. **서버 투자는 불필요**하고, 비용 0 으로 배포 조건을
만들 수 있다. 남은 것은 두 개다.

1. **6절 (A) 크론 분기를 구현할까** — 기존 패턴 무영향을 테스트로 고정해서
2. **그 뒤 `adopted_1h_patterns` 에 캐스케이드를 등재할까** — 5절 위험을 감수하고
