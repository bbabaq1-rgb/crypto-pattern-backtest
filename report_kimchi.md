# 거래소 간 프리미엄(김치 프리미엄) — 사전 등록 시험 결과

- 사전 등록: `registry.json` → `kimchi_prereg_2026_09_22`
- 실행: GitHub Actions run **35744717876** (2026-09-22), `validate_kimchi.py --no-fetch`
- **판정: 가족 X CONFIRMED 0 / REJECTED 4(REVERSED 1) · 가족 T CONFIRMED 0 / REJECTED 3**
- **음성 대조 X 0/3 · T 0/2 → 두 가족 모두 판 VALID**
- **실거래 무변경**(`DEPLOY_ON_PASS=False`)

## 0. 왜 이 시험을 했나

외부 분석 핸드오프(`ext_evidence_bitmex_aoa_2026_09_22`)가 "방향 선택을 개선하려면 **비가격 입력**이
필요하다"며 넷을 꼽았다. 레포 상태에 매핑하니 펀딩 극단은 기각 완료, 청산 캐스케이드는 대리지표로
배포, OI 는 수집만, **호가 불균형은 데이터 자체가 없고**, **거래소 간 프리미엄만 미시험이면서 데이터를
만들 수 있었다**(업비트 KRW REST 가 이 환경에서 열린다).

## 1. 데이터 — 봉 정렬이 전부였다

업비트 **일봉은 KST 기준**(00:00 KST = 15:00 UTC)이라 `data_long` 의 UTC 일봉 종가와 **9시간** 어긋난다.
알트 일변동을 5% 라 하면 9시간 표류는 약 3% 이고, **재려는 상대 김프 스프레드(약 1~3%)보다 크다** —
일봉을 그냥 썼으면 신호보다 큰 잡음을 얹는 셈이었다.

업비트 **240m 캔들은 UTC epoch 정렬**(00/04/08/12/16/20)이라 `D 20:00` 봉의 종가 = `D+1 00:00 UTC`
= `data_long` date=D 행의 종가 시각이다. 그래서 240m 을 받아 20:00 봉만 남겼다(`build_data_kimchi.py`).
코인당 하루 한 행이라 62 종목 전체가 1.6MB 다.

- 격자 **2019-05-03 ~ 2026-09-21 · 2,695일**(코인 15개 이상인 날만) · KRW∩`data_long` **62 종목**
- 코인-월 패널 **2,922** · 월 2019-05~2026-08(**88개월**, train 67 / OOS 20)
- 환율은 ECB 고시(frankfurter) 2,489행 — **가족 T 전용**

### 측정이 실제로 김프인지 — D2 가 확인해 준다

연도별 중앙 프리미엄: 2019 **+0.30%** / 2020 −0.42% / 2021 **+3.32%** / 2022 +1.53% / 2023 +1.45% /
2024 +2.10% / 2025 +1.52% / 2026 −0.05%, 최근(2026-09-21) **−1.42%**(역프).
2021 고점과 2026 역프가 알려진 김프 역사와 맞는다 — 배관이 재려던 것을 재고 있다.

## 2. 가족 X (주 판정) — 횡단면 종목 선택

`kp_rel_i = ln(KRW_i/USDT_i) − 그날 전 종목 중앙값`. 환율은 같은 날 모든 코인에 **공통 곱**이라
차감에서 정확히 소거된다(`test_kimchi §2`: 날짜별 임의 환율을 곱해도 최대차 **1.8e-15**).

| 셀 | 선언 | train IC | ICIR | Holm | trTOPe | OOS IC | oos_p | 판정 |
|---|---|---|---|---|---|---|---|---|
| kp_rel | − | −0.042 | −1.39 | 1.000 | +1.69% | −0.026 | .769 | REJECTED |
| kp_rel_chg5 | − | −0.018 | −0.63 | 1.000 | +2.18% | −0.006 | .571 | REJECTED |
| kp_rel_chg20 | − | −0.025 | −0.83 | 1.000 | −1.87% | −0.033 | .823 | REJECTED |
| kp_rel_z60 | − | **−0.057** | −2.28 | 1.000 | +2.68% | −0.033 | .850 | REJECTED **REVERSED** |

(IC 는 **선언 방향으로 정렬한 값**이다. 음수 = 선언과 반대.)

- **네 셀 전부 선언 부호와 반대**다. 원 관계는 '상대 김프가 높은 코인이 이후 **더 오른다**'이고,
  `kp_rel_z60` 은 월 부트 양측 p<.05 로 **REVERSED** 판정을 받았다.
- **그러나 train 한정이다** — OOS(2025-01~) 에서는 네 셀 모두 p .57~.85 로 무작위와 구분되지 않는다.
- 선언 부호로는 어느 셀도 C1 을 넘지 못했다(Holm 1.000).

### REVERSED 를 승격하지 않는 이유 — D5 가 정체를 말해 준다

`kp_rel` 과 xsec 24변수의 월별 순위상관 상위: **range_pos_20 −0.20 · rsi14 −0.18 · ret_1w −0.17**.
즉 **상대 김프가 높은 코인 = 최근 (자기 레인지 안에서) 많이 빠진 코인**이다. 원화 호가가 하락을
늦게 따라가면 기계적으로 프리미엄이 생긴다. 그러면 '프리미엄 높음 → 이후 더 오름'은 새 정보가 아니라
**레포가 이미 아는 단기 반전의 재포장**일 수 있다. 상관이 0.2 수준이라 전부는 아니지만, REVERSED
셀을 쫓으려면 반전 변수를 통제한 별도 사전 등록이 필요하다(`validate_ih_exit` 교훈).

## 3. 가족 T (보조 판정) — 시계열 방향

| 셀 | 선언 | train IC | p | Holm | **MDE** | **ac20** | OOS IC | oos_p | 판정 |
|---|---|---|---|---|---|---|---|---|---|
| kp_mkt | −1 | **−0.189** | .102 | .306 | −0.264 | +0.561 | −0.018 | .433 | REJECTED |
| kp_mkt_chg20 | −1 | −0.031 | .402 | .456 | **−0.179** | −0.369 | +0.139 | .969 | REJECTED |
| kp_mkt_z120 | −1 | −0.107 | .228 | .456 | −0.268 | +0.183 | +0.040 | .687 | REJECTED |
| ctrl_ar1 *(대조)* | −1 | −0.072 | .245 | — | −0.208 | +0.387 | +0.088 | .817 | REJECTED |
| ctrl_season *(대조)* | −1 | **+0.216** | .985 | — | −0.257 | +0.935 | −0.075 | .289 | REJECTED |

- **`kp_mkt` 은 선언 방향이 맞았다**(시장 전체 프리미엄이 높을수록 이후 20일 중앙 수익이 낮다,
  IC −0.189). 그런데 문턱 0.20 **바로 아래**이고, 그마저 **자기 MDE 0.264 아래**라 이 판이 애초에
  검출할 수 없는 크기다. OOS 에서도 −0.018 로 재현되지 않는다.
- **연 주기 사인파 대조가 이 가족 최대 |IC| 0.216 을 냈다** — 실제 지표 어느 것보다 크다.
  선언 부호가 반대로 나와 탈락했을 뿐, **경제적 내용이 0 인 계열이 자기상관만으로 그 정도를 찍는다.**
  알트시즌 판(잡음 대조 |IC| 0.184)에 이은 두 번째 실증이다.
- **지속성 → MDE 기전도 재현됐다**: ac20 이 가장 낮은 `chg20`(−0.369)의 MDE 가 **0.179** 로 가장 작고,
  가장 지속적인 `ctrl_season`(ac20 +0.935)이 가장 큰 |IC| 를 만든다. 검정력 곡선(`power_curve.py`)이
  예측한 그대로다.

## 4. 진단 셀 하나가 모든 층을 통과했다 — `kp_volshare`

| 셀 | dir | train IC | ICIR | p(양측) | yr+ | trTOPe | OOS IC | oos_p | ooTOPe | ooTOP |
|---|---|---|---|---|---|---|---|---|---|---|
| kp_volshare | − | **+0.080** | **2.87** | **.002** | 67% | +1.25% | **+0.066** | **.046** | +0.72% | +0.85% |

`kp_volshare = ln(업비트 KRW 거래대금 점유율 / USDT 측 점유율)` — 한국 거래 집중도. 방향이 '−' 로
정해졌으므로 읽으면 **한국 거래 집중도가 낮은 코인이 이후 20봉 더 오른다**(= 집중도가 높으면 나쁘다).
train·OOS 양쪽에서 유의하고, TOP−유니버스가 두 분할 다 양수이며, OOS TOP 절대값 0.85% 가 왕복 비용
0.4% 를 넘는다. **가족 X 주 판정 4셀이 못 넘은 층을 전부 넘었다.**

### 그래도 승격하지 않는다 — 그리고 그 이유가 사후가 아니다

- 사전 등록에서 이 셀을 **진단으로 분류한 이유는 측정 문제**였다: 업비트 쪽 거래대금은 **4h 슬라이스**,
  USDT 쪽은 **1d** 라 점유율로 정규화해도 장중 구성 차이가 남는다. **결과를 보고 강등한 것이 아니라
  결과 전에 그렇게 적어 뒀다.**
- 방향이 '?'(train 에서 선택)이라 주 판정 4셀보다 자유도가 하나 많다. train p 는 양측으로 보정했고
  OOS 는 그 방향의 재현을 요구했지만, 사전에 부호를 건 셀과 같은 값으로 읽으면 안 된다.
- 레포 규칙: **사후에 진단에서 골라 '살았다'고 하지 않는다**(`validate_ih_exit` 교훈).

**후속 후보(사용자 결정)**: 같은 슬라이스로 거래대금을 맞추고(업비트 240m 6봉 합산 vs USDT 1d),
부호를 사전에 걸어 별도 사전 등록. 방향이 맞다면 이것은 레포가 **네 번 독립 재현한** '최근 거래대금·
관심 증가 → 이후 나쁨'(xsec_chars `vol_ratio_30_90` REVERSED · episode_profile REVERSED ·
breadth_state)의 **다섯 번째 재현**이고, 재료가 국내 거래소라는 점에서 축이 새롭다.

## 5. 사전 확률 채점 (결과 전 기록과 대조)

| 적은 것 | 결과 |
|---|---|
| CONFIRMED 0 | **적중** (가족 X·T 둘 다 0) |
| 가장 있을 법한 흥미로운 결과는 REVERSED | **적중** — `kp_rel_z60` 이 정확히 그 판정을 받았다 |
| 가족 X 안에서 kp_rel(수준)이 가장 약하고 z60·chg 가 나을 것 | **절반 오답** — \|IC\| 는 z60 이 최대가 맞지만 **선언과 반대 방향**이고, 가장 약한 것은 kp_rel 이 아니라 chg5 다 |
| 가족 T 에서 뭔가 나오면 그게 실질 소득 | **안 나왔다.** 대신 방법론 소득이 나왔다(아래) |
| 크기·순위는 예측하지 않는다 | 지켰다 |

## 6. 이 판이 실제로 남긴 것

1. **김프는 이 레포의 프레임에서 종목 선택에도 방향에도 쓸 수 없다** — 주 판정 7셀 전부 기각,
   OOS 재현 0. 비가격 입력 네 축 중 셋째가 닫혔다(펀딩·캐스케이드에 이어).
2. **가격 괴리는 수요가 아니라 시차일 수 있다** — D5 가 상대 김프를 단기 하락(range_pos_20·rsi14·
   ret_1w)과 묶는다. 이 해석이 맞으면 김프의 '정보'는 이미 반전 패턴이 쓰고 있는 것이다.
3. **잡음 대조가 실제 지표를 이긴 두 번째 사례** — `ctrl_season` |IC| 0.216 > `kp_mkt` 0.189.
   대조 없이 봤으면 `kp_mkt` 의 −0.189 를 "문턱에 근접한 신호"로 읽었을 것이다.
4. **지속성 → MDE 기전의 독립 재현** — 다른 데이터·다른 타깃·다른 지평(120→20)에서 같은 순서.
5. **거래대금 축이 가격 축보다 낫다는 신호** — 같은 데이터 안에서 가격 괴리(4셀)는 전부 기각인데
   거래 집중도(1셀)는 전 층을 통과했다. 진단이라 승격하지 않지만, 다음에 팔 자리를 가리킨다.

## 7. 한계 (사전 등록에 적은 그대로)

- 이중 생존 편향: 업비트 상장폐지 코인 없음 + `data_long` 자체가 오늘 OKX 에 있는 80종목
- `data_long` 이 종목마다 다른 현물 거래소(gate/kucoin/coinbase…)라 그 가격차가 비율에 섞인다.
  중앙값 차감은 **그날 공통분**만 지우고 종목별 거래소 차이는 남는다
- 원화 입출금 제한·전송 시간 미반영 — '프리미엄이 높다'가 곧 차익 기회가 아니다(예측력만 봤다)
- 가족 T 의 ECB 고시는 14:15 CET·평일이라 00:00 UTC 와 약 10시간 어긋난다
- 정통 김프(업비트 KRW-USDT 환산)는 **그 시장이 2025-06-15 상장**이라 train 이 없다
- 러너 자동 수집이 USDT 측 최신 봉을 붙일 수 있으나 KRW 데이터가 2026-09-21 에서 끝나고
  그 구간은 fwd20 이 없어 판정에 들어가지 않는다

## 8. 공개 — 커밋 전에 본 것

수집이 끝나기 전 **부분 데이터(27종목)로 배관 스모크를 1회** 돌렸다. **종료 코드와 예외만 확인했고
수치는 보지 않았으며** 산출물(`_kimchi.json`)은 지웠다. 목적은 러너에서 처음 돌다 터져 한 사이클을
버리는 것을 막는 것이고, 판정 규칙·파라미터는 그 전후로 한 글자도 바뀌지 않았다.

---
파일: `build_data_kimchi.py` / `validate_kimchi.py` / `test_kimchi.py`(55건) /
`.github/workflows/kimchi.yml` / `data_kimchi/`(62 종목 + ECB 환율)
