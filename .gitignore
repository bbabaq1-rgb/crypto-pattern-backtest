data/
__pycache__/
*.pyc
*.pyo

# temp/working files
_*.tmp
_filter_result.json

# 로컬 환경변수 (키 포함 — 절대 커밋하지 않음)
.env
.env.local

# 페이퍼 상태 파일 — Supabase가 원천 (러너는 restore_state_db로 복원)
paper_positions.json
paper_trades.json

# 독립 git 저장소 (PII 비식별화 PoC)
pii-deid/

# ML 연구 산출물(연구전용)
_ml_*.json
