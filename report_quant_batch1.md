# 퀀트 카탈로그 1차 묶음 — 변동성 타겟팅 + 횡단면 모멘텀 (2026-09-04)

학술 카탈로그(quant_pattern_catalog.md) A등급 중 레포에서 미시험이던 2건. 사전 등록 후 실행.
run #1 (33843531875) → 문제 2건 발견 → 수정 → **run #2 (33844461829) 채택**.
run #3 (33847079467) 에서 사용자 요청으로 **실거래 라우팅 복제 판**을 추가(§1.1). 실거래 무변경.

---

## 1. 변동성 타겟팅 사이징 — **채택 (2026-09-04 사용자 결정, 실거래 반영 완료)**

현행 실거래는 `risk 1% / 손절 8% 고정`이라 명목가 = equity × 1% ÷ 0.08 로 **자산 변동성과
무관**하다. 저변동 코인과 고변동 밈코인이 같은 크기로 들어간다. 진입 시점 20봉 실현변동성
중앙이 연율 **86%**, 10~90분위가 **49~144%** 로 3배 차이가 난다.

거래 6,640건(유니버스 80, 배포 7패턴 방식D), 블록 부트스트랩 300회.

| arm | 진입 | 평균 s | CAGR | MDD | Calmar | boot CAGR | boot MDD | boot Calmar | P(ruin) |
|---|---|---|---|---|---|---|---|---|---|
| risk (현행) | 1,413 | 1.00 | +23.4% | −57.5% | 0.41 | −0.3% | −81.8% | −0.00 | **37.7%** |
| vol_raw | 1,409 | 1.06 | +44.9% | −44.7% | 1.01 | +13.1% | −73.3% | 0.18 | 13.0% |
| **vol_matched** | 1,412 | 0.99 | **+40.6%** | **−45.4%** | **0.90** | **+12.8%** | **−73.0%** | **0.18** | **14.3%** |

**사전 등록 판정 4조건 전부 통과 → ADOPT 후보**
| 조건 | 값 | |
|---|---|---|
| (a) boot Calmar 개선 | 0.18 vs −0.00 | 통과 |
| (b) boot MDD 악화 없음 | −73.0% vs −81.8% | 통과 |
| (c) P(ruin) 악화 없음 | 14.3% vs 37.7% | 통과 |
| (d) 노출 정합 0.8~1.2 | **0.98배** | 통과 |

(d)가 핵심이다. vol_matched 는 평균 노출을 현행과 맞춘 판이므로, 개선분이 레버리지가 아니라
**같은 노출 안에서의 재분배**에서 나왔다는 뜻이다. vol_raw 도 노출 1.05배로 거의 같아
두 판의 결과가 붙는다(0.18 vs 0.18).

가장 큰 변화는 수익이 아니라 **파산 확률 37.7% → 14.3%** 다. 부트스트랩 300회 중 시작자본의
50% 밑으로 떨어진 경로가 3분의 1에서 7분의 1로 준다.

### 유보 사항 (채택 전 확인 필요)
- 이 시뮬은 **모든 패턴을 80종목 전체에** 돌린 표본이다. 실거래 라우팅(engulfing→top30,
  ih·marubozu→메이저)의 복제가 아니다. method_s `--universe` 와 같은 성격의 강건성 표본이다.
  → **§1.1 에서 라우팅 복제 판을 따로 냈고, 판정은 같다.**
- 진입 6,640건 중 **5,227건이 슬롯·증거금 부족으로 스킵**됐다. MAX_POS 12 제약이 강하게
  걸린 상태의 비교다.
- 현행(risk) arm 의 boot Calmar 가 −0.00, P(ruin) 37.7% 로 나온 것 자체가 이 표본에서
  현행 사이징이 취약함을 뜻한다. sizing_study(2026-09-02)의 '위험 1%/2x, MDD중앙 −43.1%'
  와 표본·유니버스가 달라 직접 비교는 불가.

## 1.1 실거래 라우팅 복제 (`--routing`, run #3) — **판정 동일: ADOPT 후보**

위 표본은 '표본을 최대로 키운 강건성 표본'이지 **실제로 주문이 나갔을 집합이 아니다.**
사용자 지적에 따라 스케줄러의 실제 진입 조건 세 겹을 그대로 걸어 다시 쟀다.

1. **패턴별 유니버스** — `scheduler._syms_for_pattern` (engulfing·fvg = 거래대금 top30,
   inverted_hammer·marubozu = 메이저)
2. **레짐→방향 라우팅** — `direction_switch.json` 의 표. FOCUS(engulfing/fvg)에만 걸고,
   adopted_patterns(ih/marubozu)는 스케줄러와 똑같이 **레짐 게이트 없이 롱**으로 들어간다.
   레짐 미판정 구간은 스케줄러도 표를 못 만들므로 제외.
3. **정지 패턴 제외** — `universe.json` suspended_patterns (triple_bottom 1w)

표본이 **6,640건 → 991건**으로 줄었다. 어떤 셀이 왜 빠졌는지:

| 패턴 | 남은 건수 | 라우팅 제외 | 비고 |
|---|---|---|---|
| engulfing | 120 | 14 | 종목 30 |
| fvg | 706 | 396 | 종목 30 |
| engulfing_short | 61 | 121 | 종목 30 |
| **fvg_short** | **0** | **1,048** | bear fvg 는 2026-09-04 OFF, bull 두 레짐은 롱 라우팅 → 나갈 셀이 없음 |
| inverted_hammer | 83 | 0 | 메이저 6, 레짐 게이트 없음 |
| marubozu | 21 | 0 | 메이저 6, 레짐 게이트 없음 |
| triple_bottom | — | — | 정지 패턴 |

| arm | 진입 | 평균 s | CAGR | MDD | Calmar | boot CAGR | boot MDD | boot Calmar | P(ruin) |
|---|---|---|---|---|---|---|---|---|---|
| risk (현행) | 600 | 1.00 | +9.1% | −63.3% | 0.14 | +32.8% | −51.8% | 0.65 | 3.7% |
| vol_raw | 592 | 1.15 | +22.7% | −60.5% | 0.38 | +53.3% | −48.9% | 1.09 | 0.3% |
| **vol_matched** | 600 | 0.96 | **+20.0%** | **−57.4%** | **0.35** | **+49.1%** | **−45.4%** | **1.06** | **0.3%** |

**4조건 전부 통과 → ADOPT 후보 (전 표본 판과 같은 결론)**
| 조건 | 값 | |
|---|---|---|
| (a) boot Calmar 개선 | 1.06 vs 0.65 | 통과 |
| (b) boot MDD 악화 없음 | −45.4% vs −51.8% | 통과 |
| (c) P(ruin) 악화 없음 | 0.3% vs 3.7% | 통과 |
| (d) 노출 정합 0.8~1.2 | **0.96배** | 통과 |

**읽는 법 — 두 가지가 갈린다.**
- **라우팅 자체가 큰 개선이다.** 현행(risk) arm 만 놓고 봐도 boot Calmar −0.00 → **0.65**,
  P(ruin) **37.7% → 3.7%** 다. 즉 전 표본 판에서 현행 사이징이 취약해 보였던 것의 상당 부분은
  '사이징이 나쁘다'가 아니라 **라우팅이 걸러내는 셀까지 다 넣고 쟀기 때문**이다.
- **그 위에서 변동성 타겟팅은 여전히 이긴다.** boot Calmar 0.65 → 1.06, boot MDD −51.8% →
  −45.4%, 노출은 0.96배로 정합. 개선 폭은 전 표본 판보다 작지만 **방향과 판정은 같다.**
  단 P(ruin) 개선(3.7%→0.3%)은 이미 낮은 값에서의 감소라 전 표본 판의 37.7%→14.3% 만큼
  결정적이지 않다. 이 판에서 실질적 근거는 **Calmar·MDD** 쪽이다.

**이 판의 유보**
- 라우팅 표는 **현재 표를 과거 전 구간에 소급 적용**한 것이다. `direction_switch` 표 자체가
  과거 데이터로 만들어졌으므로, 이 복제는 '오늘의 규칙이 과거에 어땠나'이지 당시 실시간
  규칙의 재현이 아니다. 사이징 arm 비교(짝지음 성격)에는 세 arm 이 **같은 신호 집합**을
  쓰므로 영향이 상쇄되지만, 절대 CAGR·MDD 수준은 그만큼 낙관적으로 읽어야 한다.
- 진입 991건 중 391건이 슬롯·증거금으로 스킵. 표본이 전 표본 판의 1/7 이라 부트스트랩
  변동이 크다.
- fvg_short 가 0건이라 이 판은 **사실상 롱 위주 + engulfing_short 61건**이다.

출력: `sizing_vol.json`(전 표본) / `sizing_vol_routing.json`(라우팅 복제).

## 1.2 실거래 반영 (2026-09-04 채택)

두 판 모두 통과했고 사용자가 채택을 결정해 실거래에 넣었다.

### 규칙
    s_raw     = clip(0.80 / σ, 0.5, 2.0)     σ = 진입 봉까지만 본 20봉 실현변동성(연율)
    vol_scale = s_raw / 1.1094               ← 1.1094 = 라우팅 표본의 s_raw 평균(s_norm)
    risk_usd  = equity x 1% x regime_mult x vol_scale
    notional  = risk_usd / 0.08

배율 범위는 **0.45 ~ 1.80배**다. 손절가·레버리지는 건드리지 않는다 — 바뀌는 건 명목가뿐이다.

### 왜 상수로 두는가
연구의 `vol_matched` 는 '지금까지 본 s_raw 의 평균'(인과적 확장평균)으로 나눈다. 앞으로 나갈
거래는 그 확장평균이 이미 수렴한 지점에 있으므로, 수렴값을 상수로 쓰는 것이 실거래에서의
충실한 구현이다(생략되는 것은 표본 초기의 과도기뿐). `sizing_vol.py` 가 매 실행 이 상수와
실제 s_norm 을 비교해 0.02 이상 벌어지면 경고를 찍는다 — 유니버스·라우팅을 개편하면 재산출한다.

### 구현 위치
- **`sizing.py` 가 원본**이다. `realized_vol` / `vol_scale_raw` / `vol_scale` 이 여기 있고
  `sizing_vol.py`(연구)가 그것을 import 한다. 연구와 실거래가 같은 함수를 타지 않으면 검증한
  규칙과 실제 주문이 조용히 갈라진다 — `test_sizing_vol_live` 가 `is` 비교로 고정한다.
- `risk_based_size(..., vol_scale=)` — 연구는 `risk_frac` 을 직접 스케일했고 수식상 동일하다.
  인자를 나눈 건 로그·반환값에 어떤 배율이 걸렸는지 남기기 위해서다(테스트로 동치 확인).
- `paper_executor` 진입 경로에서 `rows`/`ei` 로 σ 를 계산해 넘긴다.
- 폴백 3종에서 **1.0**(= 채택 전과 완전히 동일): 타겟팅 off / 상수 미설정 / σ 산출 불가(봉 부족).

### 부작용 1 — 건당 달러 위험이 1% 고정이 아니게 된다
손절폭 8% 는 그대로인데 명목가만 움직이므로 `risk_usd = equity x 1% x vol_scale`, 즉 대략
**0.45~1.80%** 범위가 된다. 일정하게 유지되는 것은 달러 위험이 아니라 **변동성 기여
(명목가 x σ)** 이고, 검증에서 개선된 것도 그쪽이다. 이건 규칙의 정의이지 부작용이 아니지만,
'위험 1% 고정'이라는 종전 표현과 어긋나므로 명시해 둔다.

### 부작용 2 — 현 계좌 규모에서 고변동 신호는 주문이 안 나간다
명목가가 줄면 증거금이 OKX 최소 주문($10)에 못 미쳐 **스킵**된다. 문턱은

    최소 equity = MIN_MARGIN x leverage x stop / (risk_frac x vol_scale) = 160 / vol_scale

| vol_scale | 필요 equity | 대응 σ |
|---|---|---|
| 1.80 (최저변동) | $89 | ≤ 44%/yr |
| 1.00 | $160 | 89%/yr |
| 0.58 | **$276 (현 계좌)** | **124%/yr** |
| 0.45 (최고변동, 하한) | $355 | ≥ 178%/yr |

즉 현 계좌(equity ≈ $276)에서는 **σ > 약 124%/yr 인 신호가 통째로 스킵된다.**

**실측 (라우팅 표본 991건, run 33850593456)** — 얼마나 잘리는지 추정하지 않고 셌다.

| equity | 스킵 기준 | 스킵되는 신호 |
|---|---|---|
| $200 | vol_scale < 0.80 | 368/991 (**37.1%**) |
| $250 | < 0.64 | 201/991 (20.3%) |
| **$276 (현 계좌)** | **< 0.58** | **약 12~15%** (아래 두 행 사이) |
| $300 | < 0.53 | 83/991 (8.4%) |
| $400 이상 | < 0.40 | 0/991 (**0.0%**) |

**equity $400 을 넘으면 부작용이 완전히 사라진다** — 최저 배율이 0.45 라 그 이하로는 잘릴
신호가 없다. 반대로 계좌가 더 작아지면 급격히 나빠진다($200 에서 37%). 채택 전 규칙은
equity ≥ $160 이면 스킵이 0% 였으므로, 이 구간에서는 **거래 기회를 실제로 잃는 교환**이다.

`sizing_vol.py` 가 이 표를 매 실행 찍고, 실거래 스킵 로그에도 "이 스케일에서 주문이
나가려면 equity >= $X" 를 함께 남긴다.

**레버리지를 올리면 증거금이 줄어 해소되지만 하지 않았다.** sizing_study(2026-09-02)에서
레버리지 상향은 명목가를 바꾸지 않고 동시 노출만 키워 MDD 를 악화시켰다(2→5x 에서
CAGR 43→36%, MDD −67→−76%). 별도 결정 사항이다.

### 되돌리는 법
`sizing.VOL_TARGETING = False` 한 줄. 그러면 `vol_scale` 이 항상 1.0 을 반환해 채택 전
동작으로 정확히 돌아간다(테스트가 이 성질을 고정).

---

## 2. 횡단면 모멘텀 — **전 셀 기각 (5/5 REJECTED)**

주간 리밸런스·상위 10종목·skip-1·L 5개 사전 등록. 주 판정은 동결 게이트(±10%/20봉),
베이스라인은 신호의 레짐 구성을 따라가는 무작위 진입.

| L | n | mean | median | 레짐 베이스라인 | 엣지 | boot_p | OOS | 판정 |
|---|---|---|---|---|---|---|---|---|
| 7일 | 1,270 | −0.57% | −7.81% | −1.06% | +0.49%p | .416 | 1/4 | REJECTED |
| 14일 | 1,260 | −0.76% | −10.27% | −1.06% | +0.30%p | .428 | 1/4 | REJECTED |
| 28일 | 1,240 | −1.08% | −10.27% | −1.10% | +0.02%p | .493 | 0/4 | REJECTED |
| 56일 | 1,200 | −0.63% | −6.39% | −1.14% | +0.51%p | .406 | 1/4 | REJECTED |
| 84일 | 1,160 | −0.74% | −5.19% | −1.07% | +0.33%p | .448 | 2/4 | REJECTED |

PASSED 0 / STRICT 0. 엣지가 전부 +0.02~+0.51%p 로 미미하고 boot_p 는 0.41~0.49 로 무작위와
구분되지 않는다.

**프레임 탓 기각이 아니다.** 추세 60봉 진단도 전부 음수(−5.4~−7.8%)라, ±10%/20봉 대칭 배리어가
오른쪽 꼬리를 잘라서 기각된 게 아니다. 포트폴리오 진단에서는 **상위 10종목이 −88% 로 유니버스
동일가중(−81%)보다 못했다** — 모멘텀 순위가 오히려 나쁜 종목을 골랐다.

RS 필터 기각(2026-07-08, 레짐 통제 후 독립 엣지 소멸)과 방향이 같다. 상대강도는 필터로도
단독 진입 규칙으로도 이 시스템에서 작동하지 않는다.

### 관측 창 한계 — 확장 시도했으나 데이터가 없다
기본 판의 실제 관측 창은 **2024~2026(116~127주)** 다. 유니버스 80 은 최근 상장이 많아 대부분
900봉이고, '20종목 이상이 L+1 이력을 가진 날짜'가 2024 이후에만 성립한다.

`--min-bars 1800` 로 창을 넓히려 했으나 **조건을 만족하는 종목이 3개뿐**이라 횡단면 순위 자체가
불가능했다(신호 0건). 이건 코드 문제가 아니라 **데이터 현실**이다 — 현 유니버스로는 2024 이전
횡단면 모멘텀을 시험할 방법이 없다. 6개월~1년 뒤 이력이 쌓이면 재시험 가능하다.

---

## run #1 에서 발견해 고친 것 (방법 정정)

**(1) 노출 지표 버그 — 판정을 뒤집을 뻔했다.** vol_matched 의 평균 스케일은 0.99 로 정규화가
됐는데 노출 비율이 1.94배로 찍혔다. **달러 명목가로 쟀기 때문**이다. 성과가 좋은 arm 은 자본이
커져 명목가도 커지므로 '레버리지를 더 썼다'와 '돈을 더 벌었다'가 구분되지 않는다. 노출을
**진입 시점 레버리지(명목가/equity)** 로 바꾸자 0.98배로 정상이었다. 판정에 (d) 노출 정합
가드를 추가했다 — vol_matched 의 존재 이유가 노출 정합이므로 그게 깨지면 (a)(b)(c) 개선이
재분배가 아니라 레버리지 효과일 수 있어 판정 자체가 성립하지 않는다.

**(2) 횡단면 관측 창 미기록.** run #1 은 창을 로그에 남기지 않아 2.4년만 덮은 것을 결과에서
역산해야 했다. date_from~date_to 를 로그·JSON 에 남기고 `--min-bars` 판을 추가했다.

---

## 결론
1. **변동성 타겟팅은 채택 후보다.** 사전 등록 4조건 전부 통과, 특히 P(ruin) 37.7%→14.3%.
   디텍터가 아니라 사이징 층이라 배포 전 패턴에 동시에 걸린다. 실거래 반영은 사용자 결정.
2. **횡단면 모멘텀은 기각.** 프레임 탓도 아니고 포트폴리오로도 유니버스보다 못하다.
   관측 창이 2.4년인 건 한계이나, 창을 넓힐 데이터가 현 유니버스에 없다.

sizing_vol.py / validate_xsec_momentum.py / test_sizing_vol.py(35건) / test_xsec_momentum.py(28건)
/ quant_batch1.yml / sizing_vol.json / _xsec_momentum.json
